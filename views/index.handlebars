 <nav class="navbar bg-secondary navbar-light bg-light shadow mb-3 ">
  <a class="navbar-brand " href="/"><h2>EAT A BURGER</h2></a>
</nav>
<div class="container">
  <div class="row">
    <div class="col-md-12 text-center">
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Asperiores suscipit laudantium, excepturi, incidunt dicta animi fuga veniam sint nesciunt amet harum. Deserunt velit odio et assumenda, temporibus illum minus fuga.</p>
    </div>
  </div>
  <div class="row text-center">
    <div class="col-md-6">
      <h2>Burgers served!</h2>
      <ul>
        {{#each burger}}
        {{#unless devoured}}
        <li>
          {{burger_name}}
          <button class="devoured" data-id="{{id}}" data-devoured="{{devoured}}">
            eat!
          </button>
        </li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-6">
      <h2>Eaten Burgers!</h2>

      <ul>
        {{#each burger}}
        {{#if devoured}}
        <li>
          {{burger_name}}
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
  <hr>
  <div class="row text-center">
    <div class="col-md-12">
      <form class="create-form">

        <div class="form-group">
          <label for="ca">Order burger here:</label>
          <input type="text" id="ca" name="name">
        </div>
        <button type="submit">Order Up!</button>
      </form>
    </div>
  </div>

</div>